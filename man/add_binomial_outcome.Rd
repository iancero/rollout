% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effect_simulation.R
\name{add_binomial_outcome}
\alias{add_binomial_outcome}
\title{Create a binomial outcome from linear predictors}
\usage{
add_binomial_outcome(
  data,
  size,
  linear_col = "y_linear",
  prob_col = "y_prob",
  binom_col = "y_binom",
  include_error = FALSE
)
}
\arguments{
\item{data}{A data frame containing effect columns prefixed with \code{"."}.}

\item{size}{The number of trials for the binomial draw (the denominator). Either a single
fixed value (e.g. \code{size = 10}) or the bare name of a column in \code{data} holding a per-row
value (e.g. \code{size = n_eligible}).}

\item{linear_col}{Name of the column to store the summed linear predictor (default \code{"y_linear"}).}

\item{prob_col}{Name of the column to store probabilities (default \code{"y_prob"}).}

\item{binom_col}{Name of the column to store binomial counts (default \code{"y_binom"}).}

\item{include_error}{Logical; whether to include a \code{.error} column (if present) in the summed
linear predictor (default \code{FALSE}). A binomial outcome's variance is already implied by \code{p}
and \code{size}, so an additional individual-level residual error term is usually not part of the
intended generative model. Set to \code{TRUE} to instead sum every \code{.}-prefixed column, including
\code{.error}.}
}
\value{
A \code{tibble} with added linear predictor, probability, and binomial count columns.
}
\description{
Generates a binomial outcome by summing effects, computing probabilities via the logistic
function, and drawing a count out of a specified number of trials. This generalizes
\code{\link[=add_binary_outcome]{add_binary_outcome()}} (which is a \code{size = 1} special case of this function) to support a
denominator greater than 1 — for example, a "Reach" outcome where \code{size} is the number of
people eligible at a site-period and the resulting count is the number who received an
intervention.
}
\examples{
df <- tibble::tibble(.beta = 0.5, .u = rnorm(5))

# Fixed number of trials
add_binomial_outcome(df, size = 10)

# Per-row number of trials drawn from a column (e.g. an eligible count)
df2 <- tibble::tibble(.beta = 0.5, .u = rnorm(5), n_eligible = c(8, 12, 9, 15, 10))
add_binomial_outcome(df2, size = n_eligible)
}
